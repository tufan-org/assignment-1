# Assignment 1

This repository contains a shell script for a collaborative GitHub assignment.

## How to run the script
Open your terminal and execute the following command:
`bash script.sh`

## Expected Result
Once the feature branch is merged, running this script will output the following text to the terminal:
`Hello World!`
